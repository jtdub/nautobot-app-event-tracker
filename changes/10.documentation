[Running the Agent](../admin/agents.md), covering configuration, the approval gate, what an agent may and may not do to a ticket, and - stated plainly rather than left implied - what none of it protects against. Plus reference pages for [Agent Run](../models/agentrun.md) and [Agent Tool Call](../models/agenttoolcall.md). [The containerlab lab](../dev/lab.md) also gains a section on running [netmiko_mcp](https://github.com/ktbyers/netmiko_mcp) beside it, so the agent can SSH to the lab's SR Linux nodes and troubleshoot a real event rather than reasoning from the ticket alone; a `development/docker-compose.netmiko-mcp.yml` overlay and its three configuration files come with it.
