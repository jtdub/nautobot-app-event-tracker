[Retrieval over Closed Tickets](../admin/rag.md), covering the pgvector extension - including the managed-PostgreSQL case where the app's migration cannot create it and a superuser must, before upgrading - registering an embedding model, the Job Hook that indexes on close, backfilling, and what the feature deliberately refuses to do. Plus a reference page for [Ticket Embedding](../models/ticketembedding.md) and the [Phase 5A specification](../dev/../specs/phase-5a-rag.md).
