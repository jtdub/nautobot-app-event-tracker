`docs/admin/dashboard.md` covers the new page: what it deliberately does not do, the `dashboard` settings block, who sees which numbers and why costs follow the ticket rather than `view_llmusagerecord`, how `stats_retention_days` and `usage_retention_days` shorten the window, and how to take the full-size performance measurement by hand. The Phase 5B specification joins the nav and gains a "What the implementation changed" section, as Phase 5A has.
